# Realtime Service

This service provides an Express-based HTTP endpoint and a WebSocket server.

Run locally:

```bash
cd realtime
npm install
npm start
```

The service exposes:
- `GET /` - health check
- WebSocket server on port `3001`
