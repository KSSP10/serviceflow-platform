# Frontend - Quickstart

Install dependencies and run dev server:

```bash
cd frontend
npm install
npm run dev
```

Open the app at the URL printed by Vite (usually `http://localhost:5173`). The page will attempt to call `http://localhost:8080/health`.
